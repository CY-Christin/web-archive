import ArchiveView from '~/components/archive-view'

// 全部归档 — every saved page; ?folder= / ?tag= query params scope the view.
function ArchivePage() {
  return <ArchiveView />
}

export default ArchivePage
