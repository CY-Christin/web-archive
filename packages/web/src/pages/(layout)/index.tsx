import LibraryView from '~/components/library-view'

// Home = the whole library (all pages), browsable/searchable/filterable.
function ArchiveHome() {
  return <LibraryView />
}

export default ArchiveHome
